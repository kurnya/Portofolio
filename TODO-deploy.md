# Deploy to GitHub Pages Plan

1. **Update vite.config.js** ✓ base already '/portofolio/' good.
2. **Set git user config** (if needed).
3. **Commit minor change** (e.g., update TODO) → trigger workflow.
4. **Enable GitHub Pages** on repo (Actions source).
5. **Monitor workflow** & get live URL.

**Status:** base OK. Starting commit.

